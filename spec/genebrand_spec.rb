require 'spec_helper'

describe Genebrand do
  it 'has a version number' do
    expect(Genebrand::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
